---
description: Build the monthly Malenadu Dara brief from an ICO market report
---

Build the monthly brief using the `malenadu-dara` skill.

Report to use (path or "find it"): $ARGUMENTS

Steps:

1. Read the report — headline figures are in the first two pages plus the
   summary tables at the end. Save it under `sources/<YYYY-MM>/` if it isn't
   there already.
2. Create `editions/<YYYY-MM>/{work,out}/`.
3. Draft the forces box — up, down, and what to watch next — and the five
   numbered sections. Figures stay in Latin digits.
4. Show me the Kannada copy before building.
5. Copy `cards-monthly-kn.html` and `cards-monthly-en.html` into `work/`, fill
   them in, build the carousels, then build the WhatsApp pair from
   `template-1-summary.html` and `template-2-details.html`.
6. View every card, check size and QR, present in filename order, Kannada first.

Forces with their numbers, no verdict.
