# Keep the working tree LF on every platform. The card decks are generated by
# make_cards.py and compared against the committed copies byte for byte, so a
# checkout that flipped them to CRLF would show every line of every template as
# changed.
* text=auto eol=lf

*.png binary
*.ttf binary
